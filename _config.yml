# Hexo Configuration
## Docs: https://hexo.io/docs/configuration.html
## Source: https://github.com/hexojs/hexo/

# Site
title: 若非的日志
subtitle: '做一个有理想的人'
description: '爱编码，爱生活，爱分享。'
keywords: 博客, 若非的博客, 技术博客, 空间周边
author: 若非
language: zh-CN
timezone: ''

# URL
## If your site is put in a subdirectory, set url as 'http://example.com/child' and root as '/child/'
url: https://hi-ruofei.com
root: /
permalink: post/:abbrlink/
abbrlink: # Abbrlink config
  alg: crc32  # 算法：crc16(default) and crc32
  rep: hex    # 进制：dec(default) and he
permalink_defaults:
pretty_urls:
  trailing_index: true # Set to false to remove trailing 'index.html' from permalinks
  trailing_html: true # Set to false to remove trailing '.html' from permalinks

# Directory
source_dir: source
public_dir: public
tag_dir: tags
archive_dir: archives
category_dir: categories
code_dir: downloads/code
i18n_dir: :lang
skip_render:

# Writing
new_post_name: :year/:month/:title.md # File name of new posts
default_layout: post
titlecase: false # Transform title into titlecase
external_link:
  enable: true # Open external links in new tab
  field: site # Apply to the whole site
  exclude: ''
filename_case: 0
render_drafts: false
post_asset_folder: false
relative_link: false
future: true
highlight:
  enable: true
  line_number: true
  auto_detect: false
  tab_replace: ''
  wrap: true
  hljs: false
prismjs:
  enable: false
  preprocess: true
  line_number: true
  tab_replace: ''

# Home page setting
# path: Root path for your blogs index page. (default = '')
# per_page: Posts displayed per page. (0 = disable pagination)
# order_by: Posts order. (Order by date descending by default)
index_generator:
  path: ''
  per_page: 10
  order_by: -date

# Category & Tag
default_category: uncategorized
category_map:
tag_map:

# Metadata elements
## https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meta
meta_generator: true

# Date / Time format
## Hexo uses Moment.js to parse and display date
## You can customize the date format as defined in
## http://momentjs.com/docs/#/displaying/format/
date_format: YYYY-MM-DD
time_format: HH:mm:ss
## updated_option supports 'mtime', 'date', 'empty'
updated_option: 'mtime'

# Pagination
## Set per_page to 0 to disable pagination
per_page: 10
pagination_dir: page

# Include / Exclude file(s)
## include:/exclude: options only apply to the 'source/' folder
include:
exclude:
ignore:

# Extensions
## Plugins: https://hexo.io/plugins/
## Themes: https://hexo.io/themes/
theme: next

# Search
search:
  path: search.xml
  field: all # avaiable value: post(Default) | page | all
  content: true
  format: html # avaiable value: html(Default) | striptags | raw

# symbols_count_time:
#   symbols: true # 统计单篇文章字数
#   time: false # 取消估算单篇文章阅读时间
#   total_symbols: false # 统计站点总字数
#   total_time: false # 取消估算站点总阅读时间

# baidu_url_submit:
#   count: 5 # 提交最新的五个链接
#   host: https://ruofei.site # 百度站长平台中注册的域名
#   token: 4UeKJlbDJYleabCQ # 准入秘钥
#   path: baidu_urls.txt # 文本文档的地址， 新链接会保存在此文本文档里

# 图片懒加载
lazyload:
  enable: true
  # onlypost: false
  # loadingImg:

# Deployment
## Docs: https://hexo.io/docs/one-command-deployment
deploy:
  type: git
  repo: git@github.com:smpower/smpower.github.io.git
  branch: master
